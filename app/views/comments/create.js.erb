<% type = comment.commentable_type.underscore %>

$('.<%= type %>-errors').html("<%= j render 'shared/errors', resource: comment %>");

<% if comment.persisted? %>
  $('.<%= type %>-comments').append("<%= j render comment %>");
  $('#comment_body').val('');
<% end %>

