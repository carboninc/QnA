$('.question-errors').html("<%= j render 'shared/errors', resource: question %>");

<% unless question.errors.present? %>
  $('#question-block-' + <%= question.id %>).replaceWith("<%= j render question %>");
<% end %>
